import React from 'react';
import PropTypes from 'prop-types';
import './Photo.css';


const Photo = () => (
  <div className="Photo">
    Photo Component
  </div>
);

Photo.propTypes = {};

Photo.defaultProps = {};

export default Photo;
